/**
 * 1. Cấu trúc file và thư mục của 1 project React
 * - public/index.html: div với id root: div này sẽ render toàn bộ trang web, hay nói cách khác, chưa toàn bộ các thẻ khác của trang web
 * - index.js: câu lệnh document.getElementById('root'), sử dụng file App.js để tạo ra các thẻ con cho div root
 * - App.js: +
 * 
 * 
 * 
 * 2. Các câu lệnh liên quan trong một project Reactjs:
 * - npm i / npm install: cài các thư viện (package) cần thiết cho project hiện tại được khai báo trong file package.json/ dependencies
 * - nếu muốn cài một thư viện hay 1 package khác, thì run npm i <tên_package>@1.1.1
 * - npm start: đọc object scripts của file package.json, triển khai code trên đường dẫn http://localhost:3000
 */